# StudyManage

project/
├── data/
│   └── data.json          # 自动生成，保存所有数据
├── pages/
│   └── index.html         # 主页面 (HTML)
├── scripts/
│   └── app.js             # 所有JavaScript逻辑
├── server.js              # 后端服务器
├── package.json           # 依赖配置
└── package-lock.json      # (自动生成)
